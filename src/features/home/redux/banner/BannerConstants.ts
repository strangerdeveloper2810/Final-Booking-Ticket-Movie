import { Banner } from "./BannerType";

// EN: Initial (empty) banner list used to seed the Redux store before data is fetched.
// VI: Danh sách banner khởi tạo (rỗng) dùng để khởi tạo Redux store trước khi có dữ liệu.
export const initialBanner: Banner[] = [];
