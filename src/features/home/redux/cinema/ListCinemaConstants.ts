import { ListCinema } from "./ListCinemaType";

// EN: Initial (empty) cinema-system list used to seed the Redux store before data is fetched.
// VI: Danh sách hệ thống rạp khởi tạo (rỗng) dùng để khởi tạo Redux store trước khi có dữ liệu.
export const initialListCinema: ListCinema[] = [];
