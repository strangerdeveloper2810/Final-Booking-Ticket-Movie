import { Film } from "./FilmType";

// EN: Initial (empty) film list used to seed the Redux store before data is fetched.
// VI: Danh sách phim khởi tạo (rỗng) dùng để khởi tạo Redux store trước khi có dữ liệu.
export const initialFilmList: Film[] = [];
