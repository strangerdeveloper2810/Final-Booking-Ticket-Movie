// EN: Barrel export so consumers can `import LoadingNew from "shared/components/LoadingNew"`.
// VI: File barrel export để nơi khác có thể `import LoadingNew from "shared/components/LoadingNew"`.
import LoadingNew from "./LoadingNew";

export default LoadingNew;
