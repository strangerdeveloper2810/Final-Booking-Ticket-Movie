// EN: Barrel export so consumers can `import Header from "shared/components/Header"`.
// VI: File barrel export để nơi khác có thể `import Header from "shared/components/Header"`.
import Header from "./Header";

export default Header;
